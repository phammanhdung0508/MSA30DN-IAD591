{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pip install -r requirements.txt && python -m uvicorn main:app --reload",
    "build": "echo 'No build step for python api yet'",
    "lint": "echo 'Linting python...'"
  }
}
